__pycache__/
*.pyc
.venv/
node_modules/

# videos are local-only (size + privacy) — only text/meta is tracked
demo/**/*.mp4
recordings/**/*.mp4

.claude/
.superpowers/
.zcode/
.env
.env.*

# Sensitive config — never commit. Copy config.example.json to config.json and edit locally.
app/config.json
app/users.json
