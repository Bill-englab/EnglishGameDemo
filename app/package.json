{
  "name": "my-english-adventure",
  "version": "0.1.0",
  "private": true,
  "description": "Local chapter→level English role-play map. Flask backend + Electron shell.",
  "main": "../electron/main.cjs",
  "scripts": {
    "test": "node --test tests-js/*.test.mjs",
    "start": "electron .."
  },
  "devDependencies": {
    "electron": "^31"
  }
}
